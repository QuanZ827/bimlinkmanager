<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Stroke icon set (Lucide-like), traced from the inline SVGs in
         design/bim-link-manager-v1.html. Used via
         <Path Style="{StaticResource IconStroke}" Data="{StaticResource Icon...}" Width=".." Height=".."/>.
         The stroke colour follows the inherited text Foreground, so buttons /
         nav tabs recolour their glyphs through Foreground triggers. -->

    <Style x:Key="IconStroke" TargetType="Path">
        <Setter Property="Stroke" Value="{Binding RelativeSource={RelativeSource Self}, Path=(TextElement.Foreground)}"/>
        <Setter Property="StrokeThickness" Value="1.5"/>
        <Setter Property="StrokeStartLineCap" Value="Round"/>
        <Setter Property="StrokeEndLineCap" Value="Round"/>
        <Setter Property="StrokeLineJoin" Value="Round"/>
        <Setter Property="Fill" Value="Transparent"/>
        <Setter Property="Stretch" Value="Uniform"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- 16x16 box: magnifier -->
    <StreamGeometry x:Key="IconSearch">M2.5,7 A4.5,4.5 0 1 0 11.5,7 A4.5,4.5 0 1 0 2.5,7 M10.5,10.5 L14,14</StreamGeometry>

    <!-- 16x16 box: circular refresh arrow -->
    <StreamGeometry x:Key="IconRefresh">M13.5 8a5.5 5.5 0 1 1-1.6-3.9M13.5 1.5v3h-3</StreamGeometry>

    <!-- 16x16 box: hierarchy / project tree -->
    <StreamGeometry x:Key="IconHierarchy">M8 2v4M4 10v2H2v2h4v-2M14 10v2h-2v2H8v-4M8 6l-4 4M8 6l4 4</StreamGeometry>

    <!-- 16x16 box: notification bell -->
    <StreamGeometry x:Key="IconNotifications">M8 1.5a5 5 0 0 1 5 5v3l1.5 2h-13L3 9.5v-3a5 5 0 0 1 5-5zM6 13.5a2 2 0 0 0 4 0</StreamGeometry>

    <!-- 16x16 box: settings gear (hub + four ticks) -->
    <StreamGeometry x:Key="IconSettings">M5.5,8 A2.5,2.5 0 1 0 10.5,8 A2.5,2.5 0 1 0 5.5,8 M8 1.5v2M8 12.5v2M1.5 8h2M12.5 8h2</StreamGeometry>

    <!-- 24x24 box: 2x2 grid (BATCH nav) -->
    <StreamGeometry x:Key="IconGrid">M3,3 h7 v7 h-7 z M14,3 h7 v7 h-7 z M14,14 h7 v7 h-7 z M3,14 h7 v7 h-7 z</StreamGeometry>

    <!-- 24x24 box: chain link (LINKS nav, links toolbar) -->
    <StreamGeometry x:Key="IconLink">M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71</StreamGeometry>

    <!-- 16x16 box: folder -->
    <StreamGeometry x:Key="IconFolder">M1.5 4.5v8h13v-6.5H8L6.5 4.5z</StreamGeometry>

    <!-- 16x16 box: document with folded corner (.rvt rows, cards, dialog chip) -->
    <StreamGeometry x:Key="IconDescription">M3.5 1.5h6L13 5v9.5H3.5zM9.5 1.5V5H13</StreamGeometry>

    <!-- 16x8 box: chevron down (combos) -->
    <StreamGeometry x:Key="IconChevronDown">M0 0l8 8 8-8</StreamGeometry>

    <!-- 8x16 box: chevron right (collapsed tree rows) -->
    <StreamGeometry x:Key="IconChevronRight">M0 0l8 8-8 8</StreamGeometry>

    <!-- 16x16 box: arrow right ("Add to Queue") -->
    <StreamGeometry x:Key="IconArrowRight">M2 8h11M9 3.5 13.5 8 9 12.5</StreamGeometry>

    <!-- 12x12 box: close X (toast, banner, queue cards) -->
    <StreamGeometry x:Key="IconClose">M2 2l8 8M10 2l-8 8</StreamGeometry>

    <!-- 24x24 box: warning triangle with ! (error banner) -->
    <StreamGeometry x:Key="IconWarning">M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0zM12 9v4M11.99 17h0.02</StreamGeometry>

    <!-- 24x24 box: play triangle (filled — use with Fill, not IconStroke) -->
    <StreamGeometry x:Key="IconPlay">M5,3 L19,12 L5,21 Z</StreamGeometry>

    <!-- 24x24 box: queue rows with leading dots (Import Queue chip) -->
    <StreamGeometry x:Key="IconQueueRows">M8 6h13M8 12h13M8 18h13M2.99 6h0.02M2.99 12h0.02M2.99 18h0.02</StreamGeometry>

    <!-- 10x10 box: check mark (checkbox glyph reference) -->
    <StreamGeometry x:Key="IconCheck">M1 5l3 3 5-6</StreamGeometry>

    <!-- 24x24 box: external link (Manage Links) -->
    <StreamGeometry x:Key="IconExternalLink">M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6M15,3 L21,3 L21,9M10,14 L21,3</StreamGeometry>

    <!-- 16x16 box: plus (per-row "add to queue" action) -->
    <StreamGeometry x:Key="IconPlus">M8 3v10M3 8h10</StreamGeometry>

    <!-- 16x16 box: small filled sort triangle (static header glyph — use with Fill) -->
    <StreamGeometry x:Key="IconSortAsc">M8,5 L11.5,10.5 L4.5,10.5 Z</StreamGeometry>

    <!-- Window chrome controls (filled glyphs, unchanged) -->
    <StreamGeometry x:Key="IconWinMinimize">M5 12h14v2H5z</StreamGeometry>
    <StreamGeometry x:Key="IconWinMaximize">M5 5h14v14H5V5zm2 2v10h10V7H7z</StreamGeometry>
    <StreamGeometry x:Key="IconWinRestore">M7 5h12v12h-4v2H3V7h4V5zm2 2v2h8v8h2V7H9zm-4 4v8h8v-8H5z</StreamGeometry>
    <StreamGeometry x:Key="IconWinClose">M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z</StreamGeometry>

</ResourceDictionary>
